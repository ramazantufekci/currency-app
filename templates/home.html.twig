<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title> Currency App </title>
</head>
<body>
    <h3>Currency App </h3>
    <ul>
        {% for key,currency in currencies %}
            <li>{{ key }} : {{ currency }} </li>
        {% endfor %}
    </ul>
</body>
</html>
